{
  "name": "luna-neco-cafe-puzzle",
  "version": "1.1.0",
  "private": true,
  "description": "Standalone LUNA NECO CAFE PUZZLE, with all original images and audio bundled locally.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/game.js && node --check src/round.js && node --check src/leaderboard.js && node --check server/worker.js && node scripts/verify-assets.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs dist",
    "ranking:dev": "node scripts/serve-ranking.mjs",
    "ranking:deploy": "node scripts/check-ranking-deploy.mjs && npm exec --yes --package=wrangler@4.131.1 -- wrangler deploy --config server/wrangler.jsonc"
  }
}
